import 'package:flutter/material.dart';

class PetInfoRegisterScreen extends StatelessWidget {
  static const name = 'pet-info-register';

  const PetInfoRegisterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PetInfoRegisterScreen'),
      ),
    );
  }
}
