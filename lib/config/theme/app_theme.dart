import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData darkTheme() => ThemeData(scaffoldBackgroundColor: Colors.grey);

  static lightTheme() => ThemeData(scaffoldBackgroundColor: Colors.white);
}
